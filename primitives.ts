/**
 * * Primitives
 * In TypeScript (and JavaScript), primitives are the most basic data types that are not objects and have no methods.
 * They represent single values.
 */

let name: string = "Lucas"; //string

let age: number = 28; // number

let isADeveloper: boolean = true; //boolean

const array: number[] = [3, 4, 555, 3]; // array with numbers
